import { createClient } from 'redis';

// Create and configure the Redis client
// const client = createClient({
//   // Configuration object
//   // If you're using default settings and running Redis locally, you might not need any options.
//   // Example for local Redis server:
//   // url: 'redis://localhost:6379'
// });
// client.on('error', (err) => console.log('Redis Client Error', err));

// // Connect the client to your Redis server
// client.connect();

// // Export the client for use in other parts of your application
// export const redisClient = client;
